import React, {useState} from "react";

//import any components needed

//Import your array data to from the provided data file
import numbers from "../../../data";
  const Numbers = () => {
  // STEP 2 - add the imported data to state
  numbers[[0],1] = Numbers(1)
  numbers[[1],2] = Numbers(2)
  numbers[[2],3] = Numbers(3)
  numbers[[4],4] = Numbers(5)
  numbers[[5],5] = Numbers(6)
  numbers[[3],6] = Numbers(4)
  numbers[[6],7] = Numbers(7)
  numbers[[7],8] = Numbers(8)
  numbers[[8],9] = Numbers(9)
  numbers[[9],0] = Numbers(0)
  numbers[[10],'.'] = Numbers('.')

  return (
    <button>
    
      {Numbers(1)}
      {Numbers(2)}
      {Numbers(3)}
      {Numbers(5)}
      {Numbers(6)}
      {Numbers(4)}
      {Numbers(7)}
      {Numbers(8)}
      {Numbers(9)}
      {Numbers(0)}    
      {Numbers('.')} 
     
     
     
     
     
     
     
     
         
     });
       
      
      {/* STEP 3 - Use .map() to iterate over your array data and return a button
       component matching the name on the provided file. Pass
       it any props needed by the child component*/}
    </button>
  );
};
export default {Numbers}