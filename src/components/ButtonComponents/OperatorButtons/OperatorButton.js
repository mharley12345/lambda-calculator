import React from "react";
import Operators from "./Operators"
const OperatorButton = () => {
  return (
    <>
       {Operators}{/* Display a button element rendering the data being passed down from the parent container on props */}
    </>
  );
};
export default OperatorButton